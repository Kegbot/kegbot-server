import { createTheme } from "@mui/material/styles";

// Stock MUI theme for now. Palette, typography, and component overrides
// will layer in here as the design evolves.
export const theme = createTheme();
